def change_edit_type(ui, enable = True):
    ui.xkjck.setEnabled(enable)
    ui.sjbxk.setEnabled(enable)
    ui.tsxxk.setEnabled(enable)
    ui.zybxk.setEnabled(enable)
    ui.zyxxk.setEnabled(enable)
    ui.tsxxk.setEnabled(enable)
    ui.tsk.setEnabled(enable)
    ui.pass_score.setEnabled(enable)
    ui.yx_score.setEnabled(enable)
    ui.lh_score.setEnabled(enable)
    ui.zd_score.setEnabled(enable)
    ui.jg_score.setEnabled(enable)
    ui.bjg_score.setEnabled(enable)
    ui.hg_score.setEnabled(enable)
    ui.bhg_score.setEnabled(enable)
    ui.now_grade.setEnabled(enable)
    ui.now_semester.setEnabled(enable)
    ui.schedule_dir.setEnabled(enable)
    ui.transcript_file.setEnabled(enable)
    ui.notice_template.setEnabled(enable)
    ui.enter_year.setEnabled(enable)
    ui.notice_dir.setEnabled(enable)
    ui.schedule_dir_view.setEnabled(enable)
    ui.notice_template_view.setEnabled(enable)
    ui.transcript_view.setEnabled(enable)
    ui.notice_dir_view.setEnabled(enable)
    ui.notice_create.setEnabled(enable)